<?php
/**
 * Google reCAPTCHA v2 Configuration
 *
 * ┌───────────────────────────────────────────────────────────────────────────┐
 * │ ВНИМАНИЕ. Ключи ниже — исторические: они лежали в этом файле открытым     │
 * │ текстом и попали в историю git, поэтому SECRET считается                  │
 * │ СКОМПРОМЕТИРОВАННЫМ. Перед публикацией сайта:                             │
 * │   1. https://www.google.com/recaptcha/admin — перевыпусти пару ключей;    │
 * │   2. задай их через переменные окружения RECAPTCHA_SITE_KEY и             │
 * │      RECAPTCHA_SECRET_KEY (в конфиге веб-сервера или systemd-юните);      │
 * │   3. значения ниже замени на пустые строки.                               │
 * │ Значения из окружения всегда имеют приоритет над записанными здесь.       │
 * └───────────────────────────────────────────────────────────────────────────┘
 *
 * Как получить ключи:
 * 1. Перейди на https://www.google.com/recaptcha/admin
 * 2. Войди в свой Google-аккаунт
 * 3. Нажми "Create"
 * 4. Выбери reCAPTCHA v2 ("I'm not a robot")
 * 5. Добавь свой домен (для локальной разработки — localhost)
 * 6. Скопируй Site key и Secret key
 */

define('RECAPTCHA_SITE_KEY', getenv('RECAPTCHA_SITE_KEY') ?: '');
define('RECAPTCHA_SECRET_KEY', getenv('RECAPTCHA_SECRET_KEY') ?: '');

// Обе константы заданы — значит защиту можно применять. Если ключи вычищены
// (пустые строки), формы должны работать без виджета, а не падать: проверку
// капчи в этом случае пропускаем осознанно.
define('RECAPTCHA_ENABLED', RECAPTCHA_SITE_KEY !== '' && RECAPTCHA_SECRET_KEY !== '');

// reCAPTCHA v2 не использует scoring — только success/fail.
